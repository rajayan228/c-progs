#include<iostream>
#include<fstream>
using namespace std;
int main(){
    ifstream fin("story.txt");
    if(!fin){
        cout<<"file does not exist"<<endl;
        return 1;
    }
    int space=0;
    char ch;
    while(fin.get(ch)){
        if(ch==' '){
        space++;
        }
        
        }
        cout<<"total space= "<<space<<endl;
        return 0;
    }
    
